{
  "buildCommand": "TOTAL_DAYS=3 npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "ignoreCommand": "bash -c 'git diff --quiet HEAD^ HEAD -- frontend/ vercel.json'"
}
